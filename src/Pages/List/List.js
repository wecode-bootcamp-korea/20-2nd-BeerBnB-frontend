import React from 'react';
import RoomCardList from './Components/RoomCardList';

const List = () => {
  return (
    <div>
      {/* 컴포넌트 통합 후 div 는 변경될 예정 */}
      <RoomCardList />
    </div>
  );
};

export default List;
